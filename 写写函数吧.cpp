#include<iostream>
#inlcude<cmath>
#define_USE_MATH_DEFINES
using namespace std;
int main()
{##不想赋值，大概就是这种算术表达式
	1/(1+1/(1+(x+y)))
	x*(x*(a*x+b)+c)+d)+e
	log(1+pow(fabs((a+b)/(a-b)),10))
	sqrt(1+M_PI/2*cos(48*M_PI/180))
	1/tan((1-x*x)/(1+x*x))
	log10(a*a+a*b+b*b)
	return 0;
}